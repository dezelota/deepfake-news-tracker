#!/usr/bin/env python3
import argparse

def fetch():
    print("Fetching articles...")
    import fetcher

def summarize():
    print("Summarizing articles...")
    import summarize

def tag():
    print("Tagging articles...")
    import tagger

# set up command line arguments
parser = argparse.ArgumentParser(description="Deepfake News Tracker")
parser.add_argument("--fetch",     action="store_true", help="Fetch new articles from RSS feeds")
parser.add_argument("--summarize", action="store_true", help="Summarize articles using Ollama")
parser.add_argument("--tag",       action="store_true", help="Tag articles by category")
parser.add_argument("--all",       action="store_true", help="Run all steps")

args = parser.parse_args()

if args.all or args.fetch:
    fetch()
if args.all or args.summarize:
    summarize()
if args.all or args.tag:
    tag()

if not any(vars(args).values()):
    parser.print_help()