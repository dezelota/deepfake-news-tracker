# deepfake-news-tracker
ool that collects recent news articles about deepfakes from at least 3 sources of your choice, stores them in a local SQLite database with deduplication, and uses an LLM API (Claude, OpenAI, whatever you prefer) to classify or summarize each article
